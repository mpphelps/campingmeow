DATABASE_URL = "postgresql://campingmeow:campingmeow@localhost:5435/campingmeow_test"
SESSION_SECRET = "super-secret-test-session-key"
E2E_AUTH_BYPASS = "1"

# The test process (not the web server) drives scans directly, against a fake
# ReserveCalifornia started by the spec. The web server stays RC_API_OFFLINE=1.
RC_BASE_URL = "http://127.0.0.1:8901/"
RC_REQUEST_INTERVAL_MS = "1"
RC_RETRY_BACKOFF_MS = "1"
